import { CrossButtonProps } from './CrossButton.type';

const defaultProps: CrossButtonProps = {
  isAnimation: false,
};

export const crossButtonProps: { [key: string]: CrossButtonProps } = {
  default: defaultProps,
};
